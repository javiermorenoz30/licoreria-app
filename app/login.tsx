import { router } from 'expo-router';
import { useState } from 'react';
import { Alert, Pressable, SafeAreaView, StyleSheet, Text, TextInput } from 'react-native';
import { colors, supabase } from '@/src/core';
import { Brand } from '@/src/ui';
export default function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[busy,setBusy]=useState(false);const submit=async()=>{setBusy(true);const{error}=await supabase.auth.signInWithPassword({email:email.trim(),password});setBusy(false);if(error)return Alert.alert('No pudimos entrar',error.message);router.replace('/(tabs)/home' as any)};return <SafeAreaView style={s.screen}><Brand/><Text style={s.title}>Bienvenido de nuevo</Text><Text style={s.copy}>Ingresa para comprar, pagar y seguir tus pedidos.</Text><TextInput style={s.input} placeholder="Correo" autoCapitalize="none" keyboardType="email-address" value={email} onChangeText={setEmail}/><TextInput style={s.input} placeholder="Contraseña" secureTextEntry value={password} onChangeText={setPassword}/><Pressable style={s.primary} onPress={submit} disabled={busy}><Text style={s.primaryText}>{busy?'ENTRANDO…':'ENTRAR'}</Text></Pressable><Pressable onPress={()=>router.push('/register' as any)}><Text style={s.link}>¿No tienes cuenta? Crear cuenta</Text></Pressable></SafeAreaView>}
const s=StyleSheet.create({screen:{flex:1,backgroundColor:'#fff',padding:24,paddingTop:55},title:{fontSize:30,fontWeight:'1000',color:colors.text,marginTop:50},copy:{color:colors.muted,lineHeight:20,marginVertical:10,marginBottom:24},input:{borderWidth:1,borderColor:colors.line,borderRadius:17,padding:16,fontSize:16,marginBottom:12},primary:{backgroundColor:colors.navy,padding:17,borderRadius:17,alignItems:'center',marginTop:8},primaryText:{color:'#fff',fontWeight:'1000'},link:{textAlign:'center',color:colors.navy,fontWeight:'800',marginTop:22}});
