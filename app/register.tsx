import { router } from 'expo-router';
import { useState } from 'react';
import { Alert, Pressable, SafeAreaView, ScrollView, StyleSheet, Text, TextInput } from 'react-native';
import { colors, supabase } from '@/src/core';
import { Brand } from '@/src/ui';
export default function Register(){const[name,setName]=useState('');const[phone,setPhone]=useState('');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const submit=async()=>{if(!name||!email||password.length<6)return Alert.alert('Revisa tus datos','Nombre, correo y una contraseña de al menos 6 caracteres son requeridos.');const{error}=await supabase.auth.signUp({email:email.trim(),password,options:{data:{full_name:name,phone}}});if(error)return Alert.alert('No pudimos crear la cuenta',error.message);Alert.alert('Cuenta creada','Ya puedes iniciar sesión.',[{text:'OK',onPress:()=>router.replace('/login' as any)}]);};return <SafeAreaView style={s.screen}><ScrollView contentContainerStyle={{padding:24,paddingTop:50}} keyboardShouldPersistTaps="handled"><Brand/><Text style={s.title}>Crea tu cuenta</Text><Text style={s.copy}>Guarda direcciones, consulta pedidos y compra más rápido.</Text>{[['Nombre y apellido',name,setName],['Teléfono',phone,setPhone],['Correo',email,setEmail]] .map(([p,v,setter]:any)=><TextInput key={p} style={s.input} placeholder={p} value={v} onChangeText={setter} autoCapitalize={p==='Correo'?'none':'words'}/>)}<TextInput style={s.input} placeholder="Contraseña" secureTextEntry value={password} onChangeText={setPassword}/><Pressable style={s.primary} onPress={submit}><Text style={s.primaryText}>CREAR CUENTA</Text></Pressable></ScrollView></SafeAreaView>}
const s=StyleSheet.create({screen:{flex:1,backgroundColor:'#fff'},title:{fontSize:30,fontWeight:'1000',color:colors.text,marginTop:42},copy:{color:colors.muted,lineHeight:20,marginVertical:10,marginBottom:24},input:{borderWidth:1,borderColor:colors.line,borderRadius:17,padding:16,fontSize:16,marginBottom:12},primary:{backgroundColor:colors.orange,padding:17,borderRadius:17,alignItems:'center',marginTop:8},primaryText:{color:colors.navy,fontWeight:'1000'}});
